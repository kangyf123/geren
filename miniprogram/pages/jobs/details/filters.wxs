var filter = {
  getInterviewStage: function (type) {
    if (type === 1) {
      return '初面';
    }
    if (type === 2) {
      return '复面';
    }
    if (type === 3) {
      return '终面';
    }
  },
};
module.exports = filter;